const playerName = prompt("Who's playing?");
const toFind = Math.floor(Math.random() * 100) +1;
let playerNumber = prompt("Choose a number between 1 - 100 : ");

// for(let i = 0; i < 100 ; i++) {
//     console.log("tour n° ", i)
// }


// boucle {
    
    // je compare le nombre du joueur et le nombre de l'ordi
    // -> si les deux sont égaux ? on ecrit bravo, et on sort de la boucle
    // -> si nombre joueur < nombre de l'ordi ? c'est plus grand
    // -> si nombre joueur > nombre de l'ordi ? c'est plus petit

// }

